// =============================================================================
// IWeatherEffect — 날씨 이벤트 '효과' 계약(인터페이스)
// =============================================================================
// [담당] Core Gameplay (윤호영)  ※ 날씨 이벤트 시스템
//
// [역할] 날씨 이벤트가 발생했을 때 '무슨 일이 일어나는가'를 구현하는 계약.
//        표시(연출), 로그, 그리고 나중엔 실제 게임플레이 효과(식량↓·지연 등)를
//        각각 IWeatherEffect 구현체로 분리 → 트리거/판정과 효과를 느슨하게 결합.
//
// [경계] 트리거·판정(MinimapWeatherEventDetector)은 '발생'만, 효과는 여기 구현체가.
//        실 효과(경제·여행)는 프레임워크 API 협의 후 구현체로 추가.
// =============================================================================

/// <summary>날씨 이벤트 발생 시 실행되는 효과 1종. 구현체를 WeatherEffectDispatcher가 모아 호출한다.</summary>
public interface IWeatherEffect
{
    /// <summary>날씨 이벤트 1건에 대한 효과 적용(표시/로그/실효과 등).</summary>
    void Apply(WeatherEventOccurrence e);
}
