// =============================================================================
// PlaceableBuilding — 배치 가능한 건물 마커
// =============================================================================
// [담당] Core Gameplay (윤호영)
//
// [역할] 마을 배치 시스템에서 "이 오브젝트는 유저가 집어서 옮길 수 있는 건물"임을
//        표시하는 마커. 배치 컨트롤러가 레이캐스트로 건물을 집을 때, 맞은 콜라이더가
//        건물(이 컴포넌트 보유)인지 바닥인지 구분하는 데 쓴다.
//
// [규칙] 건물 프리팹 루트에 BoxCollider와 함께 붙는다. 피벗은 바닥-중심이라
//        회전(Y축)해도 제자리에서 돈다.
// =============================================================================

using UnityEngine;

/// <summary>유저가 집어서 옮길 수 있는 건물 마커(배치 시스템이 식별용으로 사용).</summary>
public class PlaceableBuilding : MonoBehaviour
{
    // 지금은 표시만. 나중에 종류·크기·배치제약 등을 여기 넣을 수 있음.
}
